/**
 * Module dependencies
 */

var App = require('poe-ui');

/**
 * Expose the app
 */

var app = module.exports = App('hyper-todo-ui');

/**
 * Load plugins here
 *
 * Plugins are loaded with the 'use' method:
 *
 *   app.use(require('modal'));
 *
 */
app.configure(function(injector) {

});
